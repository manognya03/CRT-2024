program:
read a number and print the number i reverse order,

source code:
#include <stdio.h>

int main() 
{
    int n,x,r=0;
    scanf("%d",&n);
    while(n>0){
       x=n%10;
       r=r*10+x;
       n=n/10;
    }
    printf("%d ",r);
    
    return 0;
}
