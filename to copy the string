problem:
program to copy the string without strcpy()

source code:
#include <stdio.h>

int main() {
    char str1[50],str2[50];
int i;
  scanf("%[^\n]s",&str1);
  for(i=0;str1[i]!=0;i++)
    {
        str2[i]=str1[i];
    }
    str2[i]='\0';
    printf("%s\n%s",str1,str2);

    return 0;
}
